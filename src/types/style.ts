export type CommonListType = {
  grid?: number;
};

export type CommonInputType = {
  length?: string;
};

export type CommonButtonType = {
  bgColor?: string;
  textColor?: string;
};

export type ShowListTableType = {
  tableGrid?: number[];
};
